{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "google-spreadsheet": "^2.0.4",
    "json-stringify-pretty-compact": "^1.0.4",
    "morgan": "^1.9.0",
    "nodemon": "^1.12.1"
  },
  "scripts": {
    "start": "node server.js",
    "watch": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd app && yarn --production=false && yarn run build"
  }
}
