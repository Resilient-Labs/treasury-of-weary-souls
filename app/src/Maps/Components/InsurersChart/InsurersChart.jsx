import React from 'react';
import './InsurersChart.css';

// let otherCount = 0;
// let count = 0;
const InsurersChart = (props) => {
    return ( 
        <div class="insurers-chart"></div>
        // <div className="insurers-charts">
        //     { Object.keys(props.insurers).map((insurer) => {
        //         if (props.insurers[insurer] > 10) {
        //             return <progress value={ props.insurers[insurer] } max={ props.insurersCount }>{ insurer }</progress>
        //         } 
        //     }) }
        // </div> 
    )
}

export default InsurersChart;